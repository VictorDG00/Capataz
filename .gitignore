# --- Python & Ambientes ---
venv/
__pycache__/
*.py[cod]
*$py.class
.ipynb_checkpoints
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- Segurança & Segredos (CRÍTICO) ---
.env
*.pem
*.key
certs/
secrets.json

# --- Banco de Dados Local (Persistência do LangGraph) ---
# Queremos que o histórico de execução fique na sua máquina, não no Git.
*.db
*.sqlite3
src/core/checkpoints.db

# --- Logs e Artefatos de Execução ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache

# --- Sistema e IDE ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- Capataz (O que DEVE subir vs O que FICA) ---
# O .capataz/ deve subir (regras e templates), 
# mas se você criar pastas de saída de teste, ignore-as:
/forja_output/